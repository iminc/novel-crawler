const fs = require('fs')
const ora = require('ora')
const { HOST, getHtml } = require('./util')

async function getChapter(book) {
    const $ = await getHtml(book.link)


}

(async () => {

    

})()